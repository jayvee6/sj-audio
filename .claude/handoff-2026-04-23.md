# SJAudio handoff — 2026-04-23

**State:** chunk 0 in progress → will be committed momentarily.
**Last landed:** none yet.
**Mid-edit files:** none — all files written, ready for commit.
**Next 3 steps:**
  1. Chunk 1 — `tsconfig.json` (ES2020, lib DOM, strict, declaration true) + `npm i -D typescript`.
  2. Chunk 2 — `rollup.config.js` (ESM+CJS+UMD, UMD global `SJAudio`) + `src/index.ts` with `version = '0.0.1'` stub + verify `dist/sj-audio.umd.js` exposes `window.SJAudio.version`.
  3. Chunk 3 — Vitest install + `test/smoke.test.ts` asserting version export.

**Verify current state:**
```
cd /Users/jdot/Documents/Development/sj-audio && git log --oneline && ls -la
```

**Budget policy:** Sonnet @ 200k for every chunk unless a chunk proves to need multi-file synthesis (then escalate to Opus + 1M, document why in this file).

**Subagent policy:** Explore @ Haiku for reads of `/Users/jdot/Documents/Development/musicplayer-viz/audio-engine.js` during port chunks (7, 8, 12, 13). Parent session writes code. Multi-file synthesis stays in parent.

**Circuit breaker:** if the same chunk fails twice, stop and classify (TRANSIENT / CONTEXT_OVERFLOW / LOGIC_DEADLOCK) per `~/.claude/CLAUDE.md` §6.

**Append protocol:** after every commit, append one line below. Format: `[HH:MM] chunk N: <one-line summary> — <sha7>`.

---

## Log

<!-- append below this line -->
[23:51] chunk 0: repo init — 30b02d2
[00:23] chunk 1: tsconfig + typescript — 14b9eb6
[00:25] chunk 2: rollup ESM+CJS+UMD build — ce142ef
[00:27] chunk 3: vitest harness — 699438d
[00:29] chunk 4: types barrel — aa15e31
[00:30] chunk 5: audioContext singleton — 20e2f97
[00:37] chunk 6: AnalyserNodeAnalyzer — a83d931
[00:40] chunk 7: melBands — 73d45b0
[00:42] chunk 8: gate + AGC — ba37b5b
[00:44] chunk 9: EMA smoothing — 3b5434e
[00:45] chunk 10: band averages — f680c97
[00:46] chunk 11: waveform downsample — 28d0a61
[00:49] chunk 12: onset detector — bfe9e0c
[00:50] chunk 13: BPM estimator — 9805251
[00:53] chunk 14: buildFrame pipeline — 49eb7c1
[07:39] chunk 15: baseSource — ab2fdd0
[07:40] chunk 16: createMediaElementSource + demo — c1613d7
[07:41] chunk 17: createMicrophoneSource + demo — 2910271
[07:43] chunk 18: createDisplayMediaSource + demo — 4428876
[09:05] chunk 19: createFileSource + demo — 0e7d58a
[09:07] chunk 20: detectCapabilities — b167c7e
[09:11] chunk 21: createAudioEngine — 437b256
[09:16] chunk 22: UMD drop-in smoke test — 0c9be3d
[09:20] chunk 23: README — fa94c8b
